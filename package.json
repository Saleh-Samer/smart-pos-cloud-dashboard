{
  "name": "smart-pos-cloud-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only, one-way remote monitoring dashboard for Smart POS. Receives a small JSON snapshot pushed from the shop's local server; never talks back to it.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
